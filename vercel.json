{
  "builds": [
    {
      "src": "bat-app/package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "npm run build"
      }
    }
  ],
  "outputDirectory": "bat-app/build",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}